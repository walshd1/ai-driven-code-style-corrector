# AI-Driven Code Style Corrector

Automatically corrects minor code style issues beyond basic linters (e.g., consistent use of ternary operators).

## Free
```yaml
- uses: walshd1/ai-driven-code-style-corrector@v1
  with:
    gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
```

## Paid (cost + 4.75%)
```yaml
- uses: walshd1/ai-driven-code-style-corrector@v1
  with:
    service_token: ${{ secrets.ACTION_FACTORY_TOKEN }}
```
